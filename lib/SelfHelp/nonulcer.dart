import 'package:flutter/material.dart';

class NonUlcerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Non-Ulcer Library'),
      ),
      body: Center(
        child: Text('Welcome to the Non-Ulcer Library!'),
      ),
    );
  }
}
